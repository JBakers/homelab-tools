#!/bin/bash
# Homelab Tools Configuration Example
# Author: J.Bakers
# Version: 3.1.0
#
# Copy this file to config.sh and customize for your homelab:
#   cp config.sh.example config.sh
#   nano config.sh

# Domain suffix voor je homelab
# Wordt gebruikt voor Web UI URLs in MOTDs
# Examples: ".home", ".local", ".lan", or leave empty for no suffix
DOMAIN_SUFFIX=".home"

# IP detectie methode
# Opties: "hostname" of "ip"
# - "hostname": gebruik hostname -I voor IP detectie
# - "ip": gebruik ip addr voor IP detectie
IP_METHOD="ip"
