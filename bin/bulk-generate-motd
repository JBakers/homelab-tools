#!/bin/bash

#!/bin/bash

# Bulk generate MOTD templates for all hosts
# Author: J.Bakers
# Version: 3.1.0

# Kleuren
CYAN='\033[0;36m'
BLUE='\033[0;34m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
BOLD='\033[1m'
RESET='\033[0m'

SSH_CONFIG="$HOME/.ssh/config"
TEMPLATES_DIR="$HOME/.local/share/homelab-tools/templates"

show_help() {
    echo -e "${BOLD}${CYAN}bulk-generate-motd - Generate MOTDs for all hosts${RESET}"
    echo ""
    echo -e "${BOLD}${CYAN}USAGE:${RESET}"
    echo "  bulk-generate-motd"
    echo "  bulk-generate-motd --all    # Auto-generate all hosts"
    echo ""
    echo -e "${BOLD}${CYAN}DESCRIPTION:${RESET}"
    echo "  Reads all hosts from ~/.ssh/config and generates MOTD"
    echo "  templates for each one interactively or with defaults."
    echo ""
    echo -e "${BOLD}${CYAN}OPTIONS:${RESET}"
    echo "  --all, -a    Auto-generate ALL hosts with auto-detection"
    echo "               Skips all prompts, overwrites existing templates"
    echo ""
    echo -e "${BOLD}${CYAN}MODES:${RESET}"
    echo "  The script will ask for each host whether to:"
    echo "  - Skip it (if MOTD already exists)"
    echo "  - Use defaults (service name = hostname, no web UI)"
    echo "  - Interactive mode (ask for details)"
    echo ""
    echo -e "${BOLD}EXAMPLE:${RESET}"
    echo "  ${GREEN}bulk-generate-motd${RESET}"
    echo ""
    echo "  Found 3 hosts in SSH config:"
    echo "  - jellyfin ... Generate? [y/n/i]"
    echo "  - pihole   ... Generate? [y/n/i]"
    echo "  - frigate  ... Generate? [y/n/i]"
    echo ""
}

if [[ "$1" == "--help" ]] || [[ "$1" == "-h" ]]; then
    show_help
    exit 0
fi

# Check for --all flag
AUTO_ALL=false
if [[ "$1" == "--all" ]] || [[ "$1" == "-a" ]]; then
    AUTO_ALL=true
fi

echo -e "${BOLD}${CYAN}╔══════════════════════════════════════════════════════════╗"
echo -e "║       📝 Bulk MOTD Generator                          ║"
echo -e "╚══════════════════════════════════════════════════════════╝${RESET}"
echo ""

if [[ "$AUTO_ALL" == true ]]; then
    echo -e "${GREEN}🚀 AUTO MODE:${RESET} Generating all hosts with auto-detection"
    echo ""
fi

# Check SSH config
if [[ ! -f "$SSH_CONFIG" ]]; then
    echo -e "${RED}✗ SSH config not found: $SSH_CONFIG${RESET}"
    echo ""
    echo -e "  Create it first with:"
    echo -e "  ${GREEN}edit-ssh${RESET}"
    exit 1
fi

# Maak templates directory
mkdir -p "$TEMPLATES_DIR"

# Lees alle hosts uit SSH config
hosts=$(grep "^Host " "$SSH_CONFIG" | awk '{print $2}' | grep -v '\*' | sort -u)
host_count=$(echo "$hosts" | wc -w)

if [[ $host_count -eq 0 ]]; then
    echo -e "${YELLOW}⚠ No hosts found in SSH config${RESET}"
    echo ""
    echo -e "  Add hosts with:"
    echo -e "  ${GREEN}edit-ssh${RESET}"
    exit 0
fi

echo -e "${BOLD}Found ${GREEN}$host_count${RESET} ${BOLD}hosts in SSH config${RESET}"
echo ""

# Counters
generated=0
skipped=0
failed=0

# Loop door hosts
for hostname in $hosts; do
    template_file="$TEMPLATES_DIR/${hostname}.sh"
    
    echo -e "${BOLD}${BLUE}Host: ${CYAN}$hostname${RESET}"
    
    # In AUTO_ALL mode: skip prompts, just generate
    if [[ "$AUTO_ALL" == true ]]; then
        echo -e "  ${YELLOW}→${RESET} Auto-generating with detection..."
        
        # Skip if exists unless we're in overwrite-all mode
        if [[ -f "$template_file" ]]; then
            echo -e "  ${YELLOW}⚠${RESET} Template exists, overwriting..."
        fi
        
        # Generate with auto-detection
        echo -e "n\ny\n" | generate-motd "$hostname" &>/dev/null
        
        if [[ -f "$template_file" ]]; then
            echo -e "  ${GREEN}✓${RESET} Generated successfully"
            ((generated++))
        else
            echo -e "  ${RED}✗${RESET} Generation failed"
            ((failed++))
        fi
        echo ""
        continue
    fi
    
    # Normal interactive mode
    # Check of template al bestaat
    if [[ -f "$template_file" ]]; then
        echo -e "  ${YELLOW}⚠${RESET} Template already exists"
        read -p "  Overwrite? (Y/n): " overwrite
        overwrite=${overwrite:-y}
        if [[ ! "$overwrite" =~ ^[Yy]$ ]]; then
            echo -e "  ${YELLOW}→${RESET} Skipped"
            ((skipped++))
            echo ""
            continue
        fi
    fi
    
    # Vraag hoe te genereren
    echo -e "  Generate MOTD for ${CYAN}$hostname${RESET}?"
    echo -e "    ${GREEN}y${RESET}) Yes, use auto-detection"
    echo -e "    ${GREEN}i${RESET}) Interactive (ask for details)"
    echo -e "    ${GREEN}n${RESET}) Skip"
    read -p "  Choice (Y/i/n): " choice
    choice=${choice:-y}
    
    case "$choice" in
        y|Y)
            # Gebruik auto-detection en defaults
            echo -e "  ${YELLOW}→${RESET} Generating with auto-detection..."
            
            # Call generate-motd met alleen hostname - laat auto-detection werken
            # Feed alleen de customize en deploy prompts met defaults
            echo -e "n\ny\n" | generate-motd "$hostname" &>/dev/null
            
            if [[ -f "$template_file" ]]; then
                echo -e "  ${GREEN}✓${RESET} Generated successfully"
                ((generated++))
            else
                echo -e "  ${RED}✗${RESET} Generation failed"
                ((failed++))
            fi
            ;;
            
        i|I)
            # Interactive mode
            echo -e "  ${YELLOW}→${RESET} Interactive mode..."
            echo ""
            generate-motd "$hostname"
            
            if [[ -f "$template_file" ]]; then
                ((generated++))
            else
                ((failed++))
            fi
            ;;
            
        *)
            echo -e "  ${YELLOW}→${RESET} Skipped"
            ((skipped++))
            ;;
    esac
    
    echo ""
done

# Samenvatting
echo -e "${BOLD}${CYAN}════════════════════════════════════════════════════════════${RESET}"
echo -e "${GREEN}✓ Generated:${RESET} $generated"
echo -e "${YELLOW}→ Skipped:${RESET}   $skipped"
if [[ $failed -gt 0 ]]; then
    echo -e "${RED}✗ Failed:${RESET}    $failed"
fi
echo -e "${BOLD}${CYAN}════════════════════════════════════════════════════════════${RESET}"
echo ""

# Bulk deploy optie
if [[ $generated -gt 0 ]]; then
    echo -e "${BOLD}Deploy all generated MOTDs to hosts?${RESET}"
    read -p "Deploy now? (Y/n): " deploy_all
    deploy_all=${deploy_all:-y}
    
    if [[ "$deploy_all" =~ ^[Yy]$ ]]; then
        echo ""
        echo -e "${BOLD}${CYAN}════════════════════════════════════════════════════════════${RESET}"
        echo -e "${BOLD}${CYAN}           🚀 Deploying MOTDs...                         ${RESET}"
        echo -e "${BOLD}${CYAN}════════════════════════════════════════════════════════════${RESET}"
        echo ""
        
        deployed=0
        deploy_failed=0
        
        for hostname in $hosts; do
            template_file="$TEMPLATES_DIR/${hostname}.sh"
            
            # Deploy alleen als template bestaat en nieuw gegenereerd is
            if [[ -f "$template_file" ]]; then
                echo -e "${BOLD}${BLUE}Deploying to: ${CYAN}$hostname${RESET}"
                
                if deploy-motd "$hostname" &>/dev/null; then
                    echo -e "  ${GREEN}✓${RESET} Deployed successfully"
                    ((deployed++))
                else
                    echo -e "  ${RED}✗${RESET} Deployment failed"
                    ((deploy_failed++))
                fi
            fi
        done
        
        echo ""
        echo -e "${BOLD}${CYAN}════════════════════════════════════════════════════════════${RESET}"
        echo -e "${GREEN}✓ Deployed:${RESET} $deployed"
        if [[ $deploy_failed -gt 0 ]]; then
            echo -e "${RED}✗ Failed:${RESET}   $deploy_failed"
        fi
        echo -e "${BOLD}${CYAN}════════════════════════════════════════════════════════════${RESET}"
        echo ""
    else
        echo -e "${YELLOW}Next steps:${RESET}"
        echo -e "  ${GREEN}list-templates${RESET}  - View all templates"
        echo -e "  ${GREEN}deploy-motd <host>${RESET} - Deploy to a specific host"
        echo ""
    fi
fi
